import React from 'react';
import { SizeManagement } from '../../../components/admin/sizeManagement';

const SizeManagementPage: React.FC = () => {
  return <SizeManagement />;
};

export default SizeManagementPage; 